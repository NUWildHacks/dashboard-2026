import { InlineSegments } from "@/app/guide/_components/InlineSegments";
import {
  saturdayDinnerOptions,
  saturdayLunchOptions,
  sundayBreakfastBagels,
  sundayBreakfastShmears,
  sundayLunchOptions,
} from "@/app/guide/_data/meal-options-data";

# Meal Options at WildHacks

These are all of the meal options at WildHacks. For food outside of meals, the WildSnacks Station is open at all times with a variety of snacks and beverages.

---

## Saturday Lunch from [Tomate](https://www.tomatefreshkitchen.com/)

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>Dietary Notes</th>
    </tr>
  </thead>
  <tbody>
    {saturdayLunchOptions.map((item, index) => (
      <tr key={`sat-lunch-${index}`}>
        <td>
          <InlineSegments segments={item.option} />
        </td>
        <td>{item.notes ? <InlineSegments segments={item.notes} /> : ""}</td>
      </tr>
    ))}
  </tbody>
</table>

---

## Saturday Dinner from [Papa John's Pizza](https://www.papajohns.com/)

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>Dietary Notes</th>
    </tr>
  </thead>
  <tbody>
    {saturdayDinnerOptions.map((item, index) => (
      <tr key={`sat-dinner-${index}`}>
        <td>
          <InlineSegments segments={item.option} />
        </td>
        <td>{item.notes ? <InlineSegments segments={item.notes} /> : ""}</td>
      </tr>
    ))}
  </tbody>
</table>

---

## Sunday Breakfast from [Einstein Bros. Bagels](https://www.einsteinbros.com/)

### Bagels

<table>
  <thead>
    <tr>
      <th>Option</th>
    </tr>
  </thead>
  <tbody>
    {sundayBreakfastBagels.map((item, index) => (
      <tr key={`bagel-${index}`}>
        <td>
          <InlineSegments segments={item.option} />
        </td>
      </tr>
    ))}
  </tbody>
</table>

### Shmears

<table>
  <thead>
    <tr>
      <th>Option</th>
    </tr>
  </thead>
  <tbody>
    {sundayBreakfastShmears.map((item, index) => (
      <tr key={`shmear-${index}`}>
        <td>
          <InlineSegments segments={item.option} />
        </td>
      </tr>
    ))}
  </tbody>
</table>

---

## Sunday Lunch from [10Q Chicken](https://www.10qchicken.com/)

<table>
  <thead>
    <tr>
      <th>Option</th>
      <th>Dietary Notes</th>
    </tr>
  </thead>
  <tbody>
    {sundayLunchOptions.map((item, index) => (
      <tr key={`sun-lunch-${index}`}>
        <td>
          <InlineSegments segments={item.option} />
        </td>
        <td>{item.notes ? <InlineSegments segments={item.notes} /> : ""}</td>
      </tr>
    ))}
  </tbody>
</table>
